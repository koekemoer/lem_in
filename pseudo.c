while (ants_at_end != nr of ants)
{
	start at beginning of ants_list;
	while (ants->next)
	{
		if (ant->at_end != 1(false))
		{
			check rooms linked to current room;
			move to next open linked room;
			break;
		}
		if (ant_room == end_room)
			at_end = true (1);

		ants = ants->next;
	}
}
